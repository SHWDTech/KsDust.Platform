<Window x:Class="Ks.Dust.Camera.MainControl.Views.UpdatePanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="UpdatePanel" Height="200" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Row="0">
            <TextBlock x:Name="LblUpdateMessage" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" TextWrapping="WrapWithOverflow"></TextBlock>
        </Grid>
        <Grid Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button x:Name="BtnConfirm" Content="确定" Height="35" Width="75" Click="OnClose" IsEnabled="False"></Button>
        </Grid>
    </Grid>
</Window>
